<!DOCTYPE html>
<html>
<body>
  <div class="container">
    <div class="container-fluid">
      <div class="row-fluid">
        <h1 align="center">Participants</h1>
        
        <br/><br/>
        <div class="col-md-3">
          <p class="lead">Search</p>
           <!-- Create a search bar that filters participants based on search criteria entered. -->
          <%= form_tag participants_path, :method => 'get' do %>
            <p>
              <%= text_field_tag :search, params[:search] %><br><br>
               <%= button_tag "Search Participants", :name => nil, id: 'course_submit',  :class => "btn btn-lg"%>
              <br><br>
              <small>Search by First name, Last name, Phone number, or Participant ID. To view all participants, leave search criteria blank and click search.</small>
            </p>
          <% end %>
          <br><br><br><br>
          <%= link_to 'Back to Admin', static_pages_adminhome_path %> 
        </div>
        <div class="col-md-9">
          <!-- Create table to display search results. -->
          <table class="table" border="1">
            <thead>
              <tr>
                <th>Participant Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Phone Number</th>
                <th>Membership Expiry Date</th>
                <th>Is Admin?</th>
              </tr>
            </thead>

            <tbody>
              <% @participants.each do |participant| %>
                <tr>
                  <td><%= link_to participant.participantID, participant%></td>
                  <td><%= participant.fname %></td>
                  <td><%= participant.lname %></td>
                  <td><%= participant.phone %></td>
                  <td><%= participant.expirydate %></td>
                  <td><%= participant.role %></td>
                </tr>
              <% end %>
            </tbody>
          </table>

        </div>

      </div>
    </div>
  </div>
</body>
</html>
